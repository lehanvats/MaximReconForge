# --- Core ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Task queue ---
arq==0.26.1
redis==5.0.8

# --- LangGraph orchestration ---
langgraph==0.2.34
langgraph-checkpoint-postgres==2.0.1

# --- LLM providers ---
openai>=1.40                    # Groq uses OpenAI-compatible API
anthropic>=0.30                 # Future: Anthropic Claude API
httpx>=0.27                     # Async HTTP client (needed by LLM SDKs)

# --- Supabase ---
supabase>=2.0

# --- Embeddings ---
voyageai>=0.2                   # Optional: used when VOYAGEAI_API_KEY is set

# --- WebSocket ---
websockets==13.1
